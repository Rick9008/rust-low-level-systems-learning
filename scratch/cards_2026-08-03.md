# 2026-08-03 sim o 填空(algo 系首打)+ sol 刺結案 + l 複讀深問 + 認知飽和事故復盤(收帳)

> 狀態:**sim o ✅ 8/8 綠**(不計時,三輪拉鋸;**紅測先行首次自主走完**)。sol_sim_m 刺 ✅ 結案(7bce2f0)。l 複讀 ✅(升級深問課)。**晚場實績(詳 §九)**:n lite ✅ 全鏈(改制:harness 英文 spec+clarify 版)|async 兩皮骨架默寫 ✅(首默 7 錯 → 兩輪自主修至 0)|口說 30 秒英文 ✅。滑帳:sim o stepper 複讀未跑、i–k 洞複掃/卡 FP/卡 TQ → 8/4 空檔。鐵律:code ✓✓ + 口說 ✓。
> 今日弧線值得記:午後跌到「LC 600 題是不是白刷了」「我是不是不該準備了」,一張 LC 對映表拉回,晚間自己修完 Kahn 收工,原話:「寫完很有成就感」。

## 一、sol_sim_m 刺結案(晨場,Claude 修、用戶出設計方向)

- 用戶質疑觸發:「tries 不是只在 priority queue 嗎?」→ 分清兩種設計:**用戶彩排版 strikes 隨票同居(entry 死帳就死,結構上無洞)vs sol/reference side table(每個插入點欠一個刪除點)**。
- 修法(保守,side table 留著配 drills 骨架):完成/放棄兩刪除點 + 死單不再進表;紅測 `tries_table_cleared_after_request_exit` 先紅後綠(`run` 拆 `run_core` 供驗內帳)。
- 設計課:用戶答「改兩層 map(rid → block slots)」= 對的方向;終點 = 計數搬進 `ReqState` 同居 → `reqs.remove` 順手帶走,「忘記清」不可表示——與同居設計殊途同歸。side table 的存在理由(票丟帳還在)與代價(刪除點自管)一體兩面。

## 二、l 複讀 → 深問課(晨-午,全程用戶提問驅動)

- **「不用防 race?這是 SPSC?」**——自己推出 sol 檔頭第一句(「SPSC ring,消費者是硬體」)。三層答案:每游標單一寫者(無鎖無 CAS 的根據)/讀對方游標 stale 只往保守方向錯(名牌−票跨硬體邊界)/真 race 由 barrier 擋(先值後訊號,store-release 硬體版)。
- **「不會有多人 submit?」**——`&mut Driver` 簽名把單寫者編進型別;真實世界解法 = NVMe per-core queue pair(= sim k 形狀),收尾句:**遇 MPSC 需求先問「能不能不要 MPSC」——複製結構比升級同步原語便宜**。
- **barrier 本體課**(hw-blocks-primer 門鈴與牆分頁 + 補課):一條指令、只做「擋住後面的寫直到前面的落地」;排空 store buffer ~幾十 ns;**≠ 刷 cache 到 DRAM**(coherent DMA 走一致性);x86 上 `Ordering::Release` 編譯成零指令但管不到 uncached/WC 路 → 驅動要顯式 wmb/dma_wmb。
- lifo 開關 = 把「completion 順序不是 contract、配對才是」變成可撥動的變因;tag 配對退化鏈三句:帶全文→白送 / 只帶 tag→自建 in-flight 表 / 連 tag 都沒有→跟硬體要 in-order 保證(clarify 題)。
- **watchdog 設計複驗**(用戶自發):「6 台 engine 用不著 priority queue」= sol 的 `deadline: [Option<u64>; 6]` 同款判斷;heap 的 lazy-delete 稅用戶在彩排版親手付過 → 昨天 sol 三課「先問 N 再選形狀」**癒合確認**。

## 三、認知飽和事故 + 數據校準(本日最值錢的一節)

- **症狀軌跡**:上午提取型思考全對(§二全是證據)→ 午後新散文解析崩(extract_cycle 同概念三輪重講、「往回走」都讀不進)→ 情緒下墜:「你對我的要求我都做不到」「LC 600/週賽 2000 是不是沒用」「我不要收工,我來不及準備了」。
- **校準數據(把「不會」打回去)**:sim o = **LC 207+1136+2050 換皮**(2050 的遞推式一字不差);extract_cycle = **LC 142 換皮**(pred 降維成鏈表 → seen[] 版找環入口);用戶在最霧的時刻**零錯背出 Floyd 兩階段**。底子問題結案。
- **三層診斷**(兩層是 Claude 的錯):①drill 檔頭 = 解法詞彙先於題目載入,倒著解碼雙倍負載;②教錯對象——對 LC 2000 的人堆新手比喻(剝洋蔥/問話鏈),每個新比喻都是額外解碼稅,正確講法一句:「207+2050 合體,寫吧」;③疲勞 = 放大器,殺「新散文解析」不殺「既有知識提取」(不對稱性即證據)。
- **制度修正**:時間標籤系統性偏短**認錯**(7/23「Claude 給錯碼表」同族)——lite 模板數字不得套用於「36 小時前才誕生的新領域首打」;**algo 系首打槽一律 45m,時間是數據不是判決**。
- **drill 檔讀法協定(新)**:測試(= LC Examples)→ 函式簽名 → 開寫 → `//!` 檔頭**寫完才讀**(它是題解,待遇同 sol)。
- **心理機制卡(用戶自問「為什麼躁→寫 code 就恢復」)**:躁 = 被動解碼 + 無回饋 + 每句讀不懂記一筆威脅;動手 = 已知模式 + 每步有叮聲 + 工作記憶被任務佔滿(反芻和執行搶同一塊記憶體)。**8/6 逃生梯:拿到陌生 spec 立刻做具體小事——clarify 五問/state 表/30 秒定界;定界句的本質 = 把面試官的散文翻成自己的題面(LC 翻譯)**。

## 四、sim o 填空實錄(三輪拉鋸,全程用戶手修)

| 輪 | 洞 | 一句話教訓 |
|---|---|---|
| v1 | **OR 閘入場(Dijkstra 慣性走火)**:relax 到就 push,`ind` 建完沒減過 → cycle 測試無限吊死;DAG 提早放行雙處理 → 假環 | 入場條件是題目語意:「等全部」= AND = indeg 歸零才入隊;LC 最短路肌肉會自動開火,認出來就能關 |
| v1 | `filter().enumerate()` 順序反 → 源頭認錯人(四個現成測試源頭全是前綴,僥倖綠) | enumerate 永遠貼著原始序列;「測試只驗我想到的事」再現 |
| v1 | 原始 indeg 餵 extract_cycle → remaining 混入無辜者,`pred = usize::MAX` 走一步 panic | helper 的輸入合約也是合約(「Kahn 結束後的 indeg」) |
| v2 | **鬆弛關進閘裡**:dis/parent 只在「減到 0 那條邊」寫 → 最後到貨者贏,答案跟 deps 書寫順序掛鉤(同圖換序 26→25、關鍵路徑斷尾) | 「每條邊都做的事」與「最後一條邊才做的事」分開寫 |
| v2 | (未爆,review 攔下)push 若共用 dis guard → 短邊最後到 = 跳過 push = 假環 | 鬆弛和入隊是兩個關注點,不共用一個 continue |
| v3 | **8/8 綠 + clippy/fmt 過**;用戶自我診斷收尾:「我把 queue push 的條件搞錯了,應該是 Kahn 而不是 Dijkstra 式」 | 終版:queue 只裝 node id、pop 讀 dis、stale 機制整組消失——code 比首版短 |

- **blast_radius 一次全對**;`n == 0` 邊界自發處理。
- **紅測先行首次自主走完**:`fake_circle_with_same_weight`(同圖換序不變量)+ `source_not_in_prefix` 先紅 → 修 → 綠。
- **測試配方兩條(入固定清單)**:①spec 的「無關自由度」(邊順序/編號重排)→ 不變量測試;②**沒有比較的賦值 = 「誰後到誰贏」紅旗** → 故意把錯的候選人排最後餵它(與 lifo 開關同思想)。
- **賽後追問(面試級)**:parent 能否餵 extract_cycle?→ 不能,且非方向問題——**parent 只記「發射過的鬆弛」,環的邊永遠不發射(源頭永不 pop);找環要用邊的存在性,不能用執行的痕跡**(cycle 測試實算:parent=[0,0,0],環隱形)。pred = predecessor(CLRS π 陣列),與 Rust 圈的 predicate 撞名。

## 五、認題提前吃掉的骨(8/4 卡變複驗)

- **AG-R widest path**:「路徑最小值最大」= 瓶頸最寬;Dijkstra 兩處變形(合成 + → min、擇優 min-heap → max-heap、起點 0 → ∞),貪心不變式同句(邊界最寬者定案,別條路瓶頸只會更小)。
- **最長路徑 NP-hard vs DAG P**:漢米爾頓藏在裡面(會解最長路 ⇒ 會解漢米爾頓);機制 = 環逼子問題記「用過誰」(2^n)+ 定義咬尾;DAG = 重複走點物理不可能 + topo 序給定案順序。面試句:「環逼子問題記歷史,DAG 讓它免記」。
- **root of trust 題外課**(deep dive 接話用):Boot ROM + eFuse 公鑰指紋 = 不用被驗的那塊;chain of trust 一級驗一級;anti-rollback(版本燒 fuse);attestation(簽「我是正品跑的韌體 hash=X」);Rust 適配理由 = no_std 裸機 + 記憶體安全無 GC。

## 六、今晚(在家)與明日(8/4 二,大塊)

- **今晚**:①sim o 複讀 15m——algo-hardware-skin 🛤️ boot 分頁 stepper 此刻解鎖,拿自己的實作逐幀對;②**n lite**(45m 槽;考點 BinaryHeap+seq 破平手、**indegree 入場閘**——今天的主洞正是它的招牌考點,視為複驗;讀法照新協定:測試→簽名→寫→檔頭最後);③舊 code 複習+口說出聲(兼英文鐵律;可夾「非圖論認題五句」:interval merge/答案二分/DSU 連通/滑動窗/EDF greedy——見 §八);④(5–10m)**async 兩皮骨架重默**:tokio 六行(`tcp_skeleton_std.rs` 的 tokio 對照半)+ 純 std block_on(park loop + `Arc<impl Wake>` → Waker)——7/23 executor challenge 的「poll 不准等」傷疤順便複驗。
- **8/4**:開機 = n lite 補打(僅若今晚未收)→ **背靠背 ×2**(各 25–30m 到 state 表+骨架,中場 15m 照 8/6 節奏;每題埋 sizing 小題用五行頭;評分表含 state 表開場 3 分鐘 + 英文抄紙/read back/編號逐答)→ 認題卡 FR → AG-R → TA → AG-T(+ FP、TQ 排最尾,砍序最先砍)→ 擠空檔:i–k 洞複掃 + 骨架默寫抽查 15m。**08:30 起(梯度開始)**。

## 八、graph/tree 之外的可考形狀(晚間提問沉澱;認題級即可,8/6 前不開新 drill)

依「硬體皮合理度」排序:

1. **heap 排程**(deadline/priority)——✅ sim n / h 題 / sim m 已全覆蓋。
2. **滑動窗聚合**(telemetry/rate limit)——✅ f 題 telemetry_aggregator 已覆蓋。
3. **interval merge/insert**(記憶體區段、MMIO 範圍重疊、DMA window 合併)——**唯一真缺口**,LC 56/57 換皮;認題五句帶過:sort by start → 掃一遍 → overlap 判準 `a.start <= b.end` → 合併取 max(end) → O(n log n)。
4. **答案二分**(sizing:「最大吞吐使得延遲 ≤ SLO」)——與五行頭 sizing 同居,認題即可。
5. **bit 操作**(mask/alignment/找第一個空位/2 冪 round-up)——endian_pack 已覆蓋大半。
6. **DSU 連通性**(interconnect link 斷線後連通查詢)——reference `ds/dsu` 現成,認題即可。
7. **greedy by deadline(EDF)/bin-packing 判斷**——AG-T 卡本身就是「greedy vs bin-packing」判準題。

**DP 裁決**:現實形態 = **DAG 上的 DP**(今天的最長路徑就是,已親手寫過)+ 頂多答案二分;**表格型 DP(背包/LCS/區間 DP)機率低**——PDF 訊號是「不考腦筋急轉彎、要 Big-O 直覺」,表格 DP 對硬體場景的皮太難穿。8/6 前不投資,遇到就老實 clarify 降維(「這是不是可以 greedy?反例存在嗎?」)。

**async + spec 象限(用戶自點,8/3 晚;tokio 與純 std 兩皮)**——R2 階段(sim i–o)零 async rep,是覆蓋表最薄的一格。底帳:executor challenge 空白手搓 7/23(oracle 5/5,主傷疤「poll 不准等」)、d 題 tokio 全場 7/25、d-std 7/26、Waker 鏈錄音 7/25。補法(不開新 45m 槽):①**8/4 背靠背題池納入 async 象限——tokio 皮或純 std 皮都在池內,盲測**;②骨架重默(今晚 ④;8/5 再默一次=taper 豁免項);③8/5 定界輪加 d 題三句(length-prefix / per-conn task / backpressure)+ 純 std 兩句(block_on = park loop + unpark Waker;poll 合約 = 不准等,回 Pending 前必先安排喚醒)。機率判讀:pad 有 tokio ⇒ 純 std executor 內部更可能以**口頭追問**(「block_on 底下怎麼動」)出現,不是 45m 手搓——7/23 的 rep + 錄音已是底,定界+默寫即右尺寸。

## 九、晚場收帳(n lite + async 兩皮;8/4 00:30 記)

### 場前插曲:意外的 AG-T 預習 + sim o critical path 用途課

- **critical path 用途**(用戶問「拿來幹嘛」):波次答「誰能同時開」,critical path 答「無限平行下最快也只能這麼快」= makespan 的實現路徑。兩用途:回報系統極限(sizing 句材料)+ **優化指向**——不在關鍵路徑上的 service 加速到 0 也不會讓開機變快。LC 對映:= LC 2050 本人。與 sim o 主洞收斂:同一趟 topo 裡「誰入隊」由 indegree 歸零管(Kahn)、「dist 怎麼更新」由鬆弛管(DP)——鬆弛沒有錯,它只是不負責排隊。
- **AG-T 題意課**(用戶:「完全看不懂在幹嘛」= 午後認知飽和同款症狀;處方同款 = 場景直覺 + 測試畫圖 + LC 對映):為什麼有樹 = **fan-in 上限逼出來的分層聚合**(statsd relay / Prometheus federation / MapReduce combiner 同形);d 死 → 孤兒 = `children(d)`,每個帶整棵活子樹,只斷一條邊、只重接一條邊。**認題句:剝掉樹皮是「容量受限指派」(bin packing 貪心殼),不是樹 DP——樹只貢獻孤兒名單、禁區(自己子樹)、空位表**。目標函數故意沉默 =「最小化什麼」必問(淺優先 vs 均衡 vs 搬移數,答案不同)。
- 三預測題實測:①孤兒帶 10 節點子樹掛 s → fan_in +10 ✗(**+1;容量帳算連線數不算載重**——界面最易誤讀格)②d 死 → parent 空一格 ✓ ③空位帳漏 leaf(節點 5 有 2 格)——「安置目標」直覺誤限縮成「本來就有孩子的中繼站」。8/4 AG-T 卡降半複驗。

### n lite 實錄(22:42–00:15 含 async 段;改制:用戶自點「要長 spec + 讓我問,像真面試」)

- **改制**:前導卡換成 `rehearsals/src/sim_n_scheduler.rs` 上半英文 spec + 打字 clarify(Claude 依考官手冊被問才答)。**clarify 三中三**:①job_id 唯一?→ 追問到「**唯一但不保證單調**」→ 自發推出要自己發 seq;②wait_event = epoll 形狀?✓(untyped wake → 醒後雙源 drain);③**deps 會不會指向已完成/未到的 job?→ 自己問進 Phase 2 主雷**(dep 的完成事件可能在過去、永不再來 = completed 集合的存在理由)。漏問:同權平手語意(probe 後補問,答 FIFO by arrival)。
- **state 表**:首版 5 項全在 DAG 側(finished set/反向鄰接/indegree/job 倉庫/ready heap),**worker 側整欄漏**(done 只給 worker id 卻沒有 owner 表)→ probe「walk me through: worker 2 done」一句補回 `owner: [Option<u64>; 4]`(存 job_id 不存 bool,否則 submit_job_done 叫不出來)。用詞課:inorder → **indegree**。
- **drill 5/5 綠 + 三洞帳**:
  1. debug 真洞(`dag_gates_priority` 紅):**「銷帳早於放行」**——`waiting.remove(&child)` 放在 `missing == 0` 外 = 轉移動作綁在事件上不綁在條件上;第二個相依完成時 `get_mut(&child).unwrap()` panic。與 sim o「鬆弛/入隊分家」**同族、同日兩現**。一句卡:**狀態轉移的動作綁在轉移條件上,不綁在事件上**。
  2. review 洞:**dispatch 單發不排空**(`if` 非迴圈)——t=0 四 job 四閒 worker 只派一台,`wait_event` 帶著三台閒人睡覺 = 四台被串行化;五測全綠因為 **submitted 順序 oracle 對平行度全盲**。面試加分句:"my tests verify ordering; utilization and table growth need a different probe."
  3. review 洞:`dependents` 用 `get` 不 `remove` = **表只進不出——早上才結案的 sim m 刺當日回鍋**;`remove` 還順手拿 owned `Vec`,borrow 更乾淨。
- sol 對照(用戶自評):register 用 `retain` 洗已完成相依 vs sol 不動 deps 純計數——**等價,retain 版不輸**;dispatch `loop` vs `while` 同構;其餘同形。
- **45m 結構裁決**(用戶問「每題都寫完整 struct 實際嗎?我沒信心壓在時間內」):①事件驅動題的函式拆分是 bus API 送的(get_job→register、free×ready→dispatch、worker_done→on_done),**不佔思考時間**;②struct = 紙上 state 表逐行謄寫(~2m),買的是防洞——R1 死因「時間不夠 code 有漏洞」的解藥之一(散裝變數是漏洞溫床);③**省的是方法化不是命名化**:state ≤3 件 flat 合法(R1 就這樣過)、≥4 件 struct 先立 + 轉移邏輯 inline + 註解分段,**抽 fn 是 refactor 不是前置投資**,口頭句 "I'll keep this inline for time; extracting `on_done` would be my first cleanup" = 加分;④Default/getter/doc comment 是 reference 教材的事。8/4 背靠背實測「spec → struct 立起」真實成本。

### async 兩皮首默:7 錯 → 兩輪自主修至 0(`scratch/tcp_server.rs` + `scratch/executor.rs`,皮二 rustc lib 驗綠)

- **皮一 tokio(2 錯)**:①use 塊全忘(tokio 路徑);②bind 行同行栽兩次:`new` → **`bind(...).await?`**(`.await?` 被點名三次才加上——頭號考點「.await 放哪」本尊)。accept/spawn/echo 身體一次寫對(`Ok(0) => break` EOF 臂標準)。
- **皮二 std block_on(5 錯)**:①`ThreadWaker(&Thread)` 借用 → **own**(Waker 要 `'static` 會跨線 clone;`Thread` 本身就是便宜把手);②**park/unpark 詞組整組掉**(寫成 `.wake()`/`.sleep()`——這對是這皮的心臟);③`Waker::from(Arc::new(...))` 轉換步缺;④`as_mut().poll(&mut cx)` 呼叫形;⑤**概念洞:`Pending` 臂 drop future 再重 pin = 進度歸零——future 本身就是那台狀態機,poll 的永遠是同一顆 pinned future,park 醒來繼續 poll**(會寫出 drop 是被腦內 borrow checker 逼的,`as_mut()` 即解)。
- **tokio 記法:兩規則一例外**:①模組樹鏡像 std(net/io/fs/sync)——永遠不用背 tokio 路徑,背 std 的換前綴;②唯一死記:`tokio::io::{AsyncReadExt, AsyncWriteExt}`(**async 方法住在 Ext 上**,無 std 對應物);③保險絲:先寫 body 讓 rustc E0433/E0599 吐路徑 = 正式技能,"I'll let the compiler remind me of the exact import" 合法,沒有考官在乎 use 塊默寫。
- **LSP 課**(用戶問「沒 LSP 是本來就該帶著的障礙嗎」):默寫段**是**——LSP 補全 = 開著答案默寫,recall 練不到;實戰段 = 對齊 pad(語法高亮+弱補全,無 rust-analyzer 級紅字/auto-import),主保險絲是編譯器。今晚的痛 = 量出「平常多少 code 是 LSP 在記」,**數字:7**。工作流洞一枚:給了 rustc smoke 沒跑——跑了三個殘錯編譯器全會指出來(「編譯器當 LSP」當場示範)。
- **7 這個數字是今晚最值錢的產出:它出現在 8/3 半夜,就不會出現在 8/6 09:15。**

### 修正案(進 8/4)

- 開機骨架默寫抽查 15m:**async 兩皮插隊第一位**(隔夜冷重默 = 真修復;凌晨磨綠不算);i–k 讓位。
- n lite 補打槽釋放;sim o stepper 複讀滑 8/4 空檔。
- 8/5 taper 重默 5m 從例行升格為**驗收**。

## 七、檔案清單(本日動過)

- `drills/src/ds/boot_planner.rs`——填空 + 8 測試(含自寫紅測 ×2)
- `reference/src/io/engine_watchdog.rs` / `rehearsals/examples/sol_sim_m_watchdog.rs` / `drills/src/io/engine_watchdog.rs`(spec 註解)——刺結案(7bce2f0)
- `scratch/cards_2026-08-02.md`——刺條目標結案(7bce2f0)
- `docs/interviews/sim-problems.md`——sim o 註記微修
- `drills/src/concurrency/job_scheduler.rs`——n lite 填空 5/5 綠(晚場)
- `scratch/tcp_server.rs` + `scratch/executor.rs`——async 兩皮默寫底稿(批改帳在 §九)
- 本檔 + `journal.md` + `docs/interviews/README.md` 逐日表 + `PROGRESS.md` + `rehearsals/README.md` 對照表 + `SCHEDULE.md` 快照——收帳同步
